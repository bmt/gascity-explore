# Jaws — senior engineer, assigned to the main app rig.
# Change "dir" to match your rig name.
dir = "my-app"
scope = "rig"
work_dir = ".gc/worktrees/{{.Rig}}/crew/jaws"
pre_start = ["{{.ConfigDir}}/scripts/worktree-setup.sh {{.RigRoot}} {{.WorkDir}} jaws --sync"]
wake_mode = "fresh"
idle_timeout = "2h"
max_active_sessions = 1
min_active_sessions = 0
